name: Check Variable and Trigger

on:
  push:
    branches:
      - main

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Set variable
        id: set_variable
        run: echo "::set-output name=my_variable::123"

  job2:
    needs: job1
    if: ${{ needs.job1.outputs.my_variable == "123" }}
    runs-on: ubuntu-latest
    steps:
      - name: Trigger next steps
        run: |
          echo "Variable matches! Triggering next steps..."
          # Add your desired actions here
