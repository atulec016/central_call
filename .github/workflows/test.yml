name: test_yaml

on: 
  push
      
jobs:
  command_test:
    runs-on: ubuntu-latest
    steps:
      - name: Code check for commit and update the branch
        run: |
          echo "Tis is test YAml, click [here](https://google.com)" >> $GITHUB_STEP_SUMMARY
